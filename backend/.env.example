# ============================================================
# 本地开发用。复制成 .env 后填入你自己的值。
#
# 只需要写两类东西:
#   1. 机密         —— config.yaml 里故意留空,不填启动会失败
#   2. 本机特有值   —— 你的环境和 config.yaml 默认值不一样的部分
#
# 其余所有配置项(端口、Kafka topic、连接池大小等)都在
# configs/config.yaml 里有默认值,本地开发不用在这里重复。
#
# 需要临时覆盖某一项时,直接在这里加一行即可 ——
# 每个字段都有对应的 env tag,查 internal/config/config.go 就能看到。
# ============================================================

# ---- 机密(必填) ----
DB_PASSWORD=your_mysql_password_here

# 至少 16 个字符,否则启动校验不通过
JWT_SECRET=change-me-to-a-long-random-string-please

# ---- 本机特有(按需取消注释) ----
# Redis 本地一般无密码,有的话再填
# REDIS_PASSWORD=

# MySQL 用户名/库名和默认值不同时才写
# DB_USER=root
# DB_NAME=live_stream

# 想临时开 debug 日志看 SQL 时才打开
# LOG_LEVEL=debug
# DB_LOG_LEVEL=info